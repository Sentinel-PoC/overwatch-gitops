---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: defectdojo-credentials
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: defectdojo
    creationPolicy: Owner
  data:
    - secretKey: DD_SECRET_KEY
      remoteRef:
        key: defectdojo
        property: secret_key
    - secretKey: DD_ADMIN_PASSWORD
      remoteRef:
        key: defectdojo
        property: admin_password
    - secretKey: DD_CREDENTIAL_AES_256_KEY
      remoteRef:
        key: defectdojo
        property: credential_aes_256_key
    - secretKey: METRICS_HTTP_AUTH_PASSWORD
      remoteRef:
        key: defectdojo
        property: metrics_http_auth_password
    - secretKey: DD_DATABASE_PASSWORD
      remoteRef:
        key: defectdojo
        property: db_password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: defectdojo-postgresql-specific
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: defectdojo-postgresql-specific
    creationPolicy: Owner
  data:
    - secretKey: postgresql-password
      remoteRef:
        key: defectdojo
        property: db_password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: defectdojo-postgresql
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: defectdojo-postgresql
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: defectdojo
        property: db_password
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: defectdojo-valkey-specific
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: defectdojo-valkey-specific
    creationPolicy: Owner
  data:
    - secretKey: valkey-password
      remoteRef:
        key: defectdojo
        property: valkey_password
---
# OPS-872: Keycloak credential entries removed (Keycloak decommission, OPS-867).
# Only Authentik OIDC credentials remain.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: defectdojo-extrasecrets
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: defectdojo-extrasecrets
    creationPolicy: Owner
  data:
    # Authentik OIDC backend credentials (OPS-605 Phase 3 → OPS-872 sole provider)
    - secretKey: DD_SOCIAL_AUTH_OIDC_KEY
      remoteRef:
        key: authentik/clients/defectdojo
        property: client_id
    - secretKey: DD_SOCIAL_AUTH_OIDC_SECRET
      remoteRef:
        key: authentik/clients/defectdojo
        property: client_secret
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: harbor-pull-secret
  namespace: defectdojo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: harbor-pull-secret
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "harbor.208.haist.farm": {
                "auth": "{{ printf "admin:%s" .password | b64enc }}"
              }
            }
          }
  data:
    - secretKey: password
      remoteRef:
        key: harbor
        property: admin_password
