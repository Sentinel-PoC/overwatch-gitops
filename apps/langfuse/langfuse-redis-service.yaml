apiVersion: v1
kind: Service
metadata:
  name: langfuse-redis-primary
  namespace: langfuse
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/instance: langfuse
