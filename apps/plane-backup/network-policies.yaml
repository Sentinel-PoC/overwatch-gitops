# OPS-481: NetworkPolicies for plane-backup namespace.
# Two egress targets: plane-postgresql Service (TCP 5432) + MinIO .58 (TCP 9000).
# kubectl exec NOT used — pg_dump connects directly via TCP.
# plane namespace has no default-deny today; if one is added later, an ingress
# allow on plane-postgresql from plane-backup namespace will be needed.
---
# Default deny all — defense in depth for the backup namespace
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: plane-backup
  labels:
    security.sentinel/layer: network-policy
    security.sentinel/type: default-deny
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
# Allow egress: PostgreSQL (plane ns), MinIO (.58), DNS
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-plane-backup-egress
  namespace: plane-backup
  labels:
    security.sentinel/layer: network-policy
spec:
  podSelector:
    matchLabels:
      app: plane-backup
  policyTypes:
    - Egress
  egress:
    # PostgreSQL in plane namespace (direct TCP — no kubectl exec)
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: plane
      ports:
        - port: 5432
          protocol: TCP
    # MinIO S3 backup destination (.58)
    - to:
        - ipBlock:
            cidr: 192.168.12.58/32
      ports:
        - port: 9000
          protocol: TCP
    # DNS resolution
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
        - port: 5353
          protocol: UDP
        - port: 5353
          protocol: TCP
