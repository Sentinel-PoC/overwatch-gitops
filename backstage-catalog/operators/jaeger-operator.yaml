---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: jaeger-operator
  description: Jaeger Operator managing distributed tracing instances in the cluster
  annotations:
    backstage.io/kubernetes-label-selector: name=jaeger-operator
    backstage.io/kubernetes-namespace: observability
    argocd/app-name: jaeger
  tags:
    - operator
    - tracing
    - observability
    - overwatch
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: sentinel-platform
