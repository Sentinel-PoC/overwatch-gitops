---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: openshift-authentication
  description: OKD cluster OAuth configuration — wires Keycloak/Authentik as the identity provider for cluster login
  annotations:
    backstage.io/kubernetes-namespace: openshift-config
    argocd/app-name: openshift-authentication
  tags:
    - security
    - authentication
    - oauth
    - platform
    - overwatch
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: sentinel-platform
  dependsOn:
    - component:keycloak
