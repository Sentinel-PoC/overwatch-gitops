---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: falco
  description: Falco runtime security DaemonSet — detects anomalous syscall activity and container escapes
  annotations:
    backstage.io/kubernetes-label-selector: app.kubernetes.io/name=falco
    backstage.io/kubernetes-namespace: falco-system
    argocd/app-name: falco
  tags:
    - security
    - runtime-security
    - detection
    - overwatch
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: sentinel-platform
