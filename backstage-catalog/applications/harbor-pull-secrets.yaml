---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: harbor-pull-secrets
  description: Harbor pull secrets distributor — ExternalSecret resources that provision Harbor registry credentials across platform namespaces
  annotations:
    backstage.io/kubernetes-namespace: backstage
    argocd/app-name: harbor-pull-secrets
  tags:
    - registry
    - secrets
    - platform
    - overwatch
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: sentinel-platform
  dependsOn:
    - component:harbor
